targets:
  $default:
    builders:
      source_gen:combining_builder:
        generate_for:
          include:
            - lib/domain/*.dart
            - lib/utils/file_replacements.dart
          exclude:
              - '**/example/**/*.dart'

      autoequal_gen:
        generate_for:
          include:
            - lib/domain/*.dart
            - lib/utils/file_replacements.dart
          exclude:
            - '**/example/**/*.dart'