/// the value to be used for when accessing the current value of an array
const String kIndexValue = '_INDEX_VALUE_';

/// the default number of braces to wrap a variable with
const int kDefaultBraces = 3;
